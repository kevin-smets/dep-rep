#!/usr/bin/env node
'use strict';

const argv = require('yargs').argv;
const depRep = require('../lib/depRep');

if (argv.v || argv.version) return console.log(require('../package').version);

depRep.analyze(null, true);